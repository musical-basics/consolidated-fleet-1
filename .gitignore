# Secrets / tokens — never commit
*.env
!*.env.template
.git-credentials
auth-profiles.json
openclaw.json

# Ephemeral / runtime artifacts
*.log
*.pid
.DS_Store
node_modules/
/tmp/

# OpenClaw runtime state not meant for source control
canvas/
logs/
tasks/
identity/runtime/

# Memory "dreams" / scratch
**/.dreams/
